# AI 助手完整设计建议

## 目标与安全边界

AI 助手是与“轮询设置”“打卡日历”“签到日志”同级的功能区。它接受文字、图片和文件，输出结构化的日历修改建议。AI **不能直接写入日历**：服务端先校验建议，网页展示差异预览，用户明确确认后才使用当前日历 revision 写入；revision 已变化时拒绝覆盖并要求重新生成。

AI 永远不能读取或接收密码、Cookie、PushPlus Token、恢复码及其哈希。提示词只包含当前用户主动选择的日历、坐标组和附件内容。

## 推荐的第一版数据流

1. 浏览器本地预处理附件，不把原文件永久上传。
2. 图片在浏览器缩放到最长边 1600px，并转为 WebP/JPEG；单张处理后不超过 2MB。
3. TXT、CSV、JSON、ICS 直接在浏览器提取文本；PDF 使用 PDF.js 提取文字与用户选择的页面截图；XLSX 使用 SheetJS 转为结构化表格。DOCX 可在第二阶段用 Mammoth 增加。
4. Worker 只在一次请求中把提取结果和必要图片转发给模型 API。请求结束后内存释放，不写入 D1 或 R2。
5. 模型返回受约束的日历操作，例如 `add_single_task`、`add_repeat_task`、`add_course`，不能返回任意数据库语句。
6. Worker 对日期、时间、用户名称、坐标组引用、数量上限和 JSON 大小进行校验，再返回差异预览。
7. 用户点击“应用改动”后，Worker 使用乐观锁 revision 写入；AI 只改变当前登录用户的数据，不得删除或修改其他用户数据。

## 文件与免费额度策略

第一版采用“零文件持久化”：原附件不进入 D1，也不依赖 R2，因此不会因用户不断上传文件而吃满免费存储。D1 只保存对话文本、生成的结构化变更、附件名称/MIME/字节数/SHA-256 摘要；保留最近 50 条消息或 30 天，先到者触发清理。

建议限制为每次最多 3 个文件、原始总计 15MB、单图 5MB、处理后发给模型总计 6MB。禁止 ZIP、可执行文件、脚本和未知 MIME；文件名只作显示，不作路径。若模型供应商要求 Files API，上传后必须在 `finally` 中删除远端临时文件。

R2 只作为以后可选的“保留附件”功能：用户明确勾选才保存，并配置 7 天自动删除。它不应成为首版依赖，也不应阻塞免费部署。

## 模型供应商架构

实现统一适配器：`generateCalendarPatch(messages, attachments, schema)`。首选支持 OpenAI-compatible API 的供应商配置，同时保留 Cloudflare Workers AI 适配器。API Key 只存 Worker Secret，绝不进入 D1 或网页。

管理员配置供应商、模型、全站每日额度；普通用户有每分钟和每日配额。模型必须支持结构化 JSON 输出；图片理解只在选择支持视觉的模型时启用。调用失败、超额或输出不合法时不修改日历。

## 建议的数据表

- `ai_conversations`: account_id、标题、创建/更新时间。
- `ai_messages`: role、纯文本、附件元数据、token/费用统计、30 天到期时间。
- `ai_change_sets`: base_calendar_revision、结构化操作、校验状态、应用时间。
- `ai_usage_daily`: account_id、日期、请求数、输入/输出量。

所有查询强制绑定当前 session 的 account_id。管理员只看用量和错误汇总，不看聊天正文与附件内容。

## 分阶段实现

1. 文字聊天 + 结构化日历 patch + 差异预览 + 手动应用。
2. 图片和 TXT/CSV/JSON/ICS，保持零文件持久化。
3. PDF/XLSX 本地提取，增加页码和表格预览。
4. 可选 R2 短期附件、DOCX、管理员模型与额度面板。

## 关键验收项

- AI 修改只能作用于当前用户，服务端不能只依赖提示词约束。
- 日历 revision 冲突不会静默覆盖。
- 上传原文件在请求结束后不可从服务端再次下载。
- 敏感凭据不会进入模型请求、日志或 Admin API。
- 恶意文件、超限文件、错误 JSON 和模型越权指令均被拒绝。
